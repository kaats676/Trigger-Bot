name: Build TriggerBot Jar

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'microsoft'

    # ОФИЦИАЛЬНЫЙ ШАГ: Сам разворачивает Gradle и генерирует чистый враппер
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4
      with:
        gradle-version: 8.8

    # Автоматическая генерация файлов враппера на стороне сервера
    - name: Generate Wrapper Files
      run: gradle wrapper

    # Сборка официальным изолированным методом Fabric через созданный gradlew
    - name: Build Mod with Gradle Wrapper
      run: ./gradlew build

    - name: Upload Finished Jar
      uses: actions/upload-artifact@v4
      with:
        name: TriggerBot-Fabric-1.21.1
        path: build/libs/*-snapshot.jar
