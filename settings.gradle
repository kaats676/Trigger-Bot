pluginManagement {
    repositories {
        maven {
            url = uri("https://fabricmc.net")
        }
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'triggerbot'
