pluginManagement {
    repositories {
        maven {
            name = 'Fabric'
            url = 'https://maven.fabricmc.net'
        }
        mavenCentral()
        gradlePluginPortal()
    }
}

// Ниже должен идти ваш текущий код из settings.gradle, например:
rootProject.name = 'Trigger-Bot'
